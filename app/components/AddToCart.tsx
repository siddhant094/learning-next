'use client';
import React from 'react';

const AddToCart = () => {
    return (
        <div>
            <button onClick={() => console.log('Click')}>Add to Card</button>
        </div>
    );
};

export default AddToCart;
